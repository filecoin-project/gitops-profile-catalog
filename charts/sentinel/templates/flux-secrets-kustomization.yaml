---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: sentinel-secrets
  namespace: {{ template "sentinel.namespace" . }}
  labels:
    {{- include "sentinel.labels" . | nindent 4 }}   
spec:
  interval: 5m0s
  url: {{ .Values.secrets.url }}
  ref:
    branch: {{ .Values.secrets.branch }}
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: sentinel-secrets
  namespace: {{ template "sentinel.namespace" . }}
  labels:
    {{- include "sentinel.labels" . | nindent 4 }}   
spec:
  interval: 5m
  {{ if ((.Values.secrets).serviceAccountName) }}
  serviceAccountName: {{ .Values.secrets.serviceAccountName }}
  {{ end }}
  decryption:
    provider: sops
    secretRef:
      name: wge-profiles-sops-gpg
  path: {{ .Values.secrets.path }}
  prune: true
  sourceRef:
    kind: GitRepository
    name: sentinel-secrets
    namespace: {{ template "sentinel.namespace" . }}
  timeout: 2m
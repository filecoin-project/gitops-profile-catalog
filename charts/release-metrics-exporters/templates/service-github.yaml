apiVersion: v1
kind: Service
metadata:
  name: github-exporter
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: {{ .Values.github.listenPort}}
    protocol: TCP
  selector:
    app: github-exporter